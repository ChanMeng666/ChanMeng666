# Intermediate Typst output and dev-time artifacts.
# The canonical PDF + GEO siblings live in /public and ARE tracked.
.typst-cache/
dist/
*.log

# NOTE: exports/ is deliberately TRACKED — it holds manual-upload deliverables
# (the ATS resume PDF), not build scratch. Do not add it here.
