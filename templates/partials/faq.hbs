<!-- ═══════════════════════════════════════════════════════════════════════
     SECTION: Frequently Asked (visible Q&A)
     GEO research: 41% of pages cited by LLMs carry question-shaped H3
     headings. The same FAQ data is ALSO emitted inside the hidden JSON-LD
     FAQPage block — this partial surfaces it for human readers + visual
     crawlers (Bing, Perplexity citation cards) and as semantic H3 anchors.
     Wrapped in <details> to keep the README hero clean while remaining
     visible to all crawlers.
     ═══════════════════════════════════════════════════════════════════════ -->

{{#if _faq.length}}
## Frequently Asked

<details>
<summary><strong>Open the FAQ</strong> &nbsp;·&nbsp; {{_faq.length}} questions employers / collaborators have actually asked</summary>

{{#each _faq}}
### {{question}}

{{answer}}

{{/each}}
</details>

{{/if}}
