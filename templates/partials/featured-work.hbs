<!-- ═══════════════════════════════════════════════════════════════════════
     Client & organisation work — big editorial cards (vertical stack) for the
     production systems Chan led for companies/nonprofits. Sourced from
     flagshipProjectIds (all provenance:client). Per-card: logo (right-float
     80px) · H3 name · "For <org>" line · impact · 18px tech-stack icons.
     ═══════════════════════════════════════════════════════════════════════ -->

## Client & organisation work

<sub>Production systems I led end-to-end for companies and nonprofits.</sub>

{{#each _flagshipProjects}}{{#if logo}}<img src="{{logo}}" align="right" height="80" alt="{{name}}" title="{{name}}">

{{/if}}
### [{{briefTagline name}}]({{default url repoUrl}})

{{#if _affiliationOrg}}<sub>For <a href="{{_affiliationOrg.url}}"><picture><source media="(prefers-color-scheme: dark)" srcset="{{default _affiliationOrg.logoDark _affiliationOrg.logoLight}}"><img src="{{_affiliationOrg.logoLight}}" height="22" alt="{{_affiliationOrg.name}}"></picture> {{_affiliationOrg.name}}</a>{{#if _affiliationOrg.context}} — {{_affiliationOrg.context}}{{/if}}.</sub>
{{else}}{{#if _contextLogo.length}}<sub>For {{#each _contextLogo}}<a href="{{url}}"><img src="{{src}}" height="22" alt="{{alt}}" style="vertical-align:middle"> {{alt}}</a>{{#unless @last}} · {{/unless}}{{/each}}.</sub>
{{else}}{{#if entity}}<sub>For {{entity}}.</sub>
{{/if}}{{/if}}{{/if}}

{{#if narrative.impactHeadline}}{{narrative.impactHeadline}}

{{else}}{{briefTagline tagline}}

{{/if}}{{#if _featuredStackResolved.length}}{{#each _featuredStackResolved}}<img src="{{src}}" height="18" alt="{{name}}" title="{{name}}" style="vertical-align:middle"> {{/each}}
{{/if}}{{#if _relatedProject}}
<sub>↳ Also built: <a href="{{default _relatedProject.url _relatedProject.repoUrl}}">{{briefTagline _relatedProject.name}} ↗</a></sub>
{{/if}}
<br clear="all">

{{/each}}
