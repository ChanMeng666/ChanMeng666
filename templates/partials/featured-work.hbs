{{!-- ═══════════════════════════════════════════════════════════════════════
     What I build — big editorial cards (vertical stack) for the five projects
     Chan owns herself (provenance: personal). Sourced from flagshipProjectIds.
     Verified 2026-07-15 against the shards: all five are live (status
     production/active/live); four have a public repoUrl (google-news-mcp,
     echook, archlang, vitex→easy-resume) and archcanvas is hosted with no
     public repo.
     2026-07-16: the "All five are live; four are open source" count sentence
     was REMOVED from the sub line in the plain-language pass (user-approved) —
     it had shipped a false open-source count three times. If you ever re-add a
     count, RE-READ those fields first. she-sharp used to be here; it is
     client work (provenance: client, NZ-SheSharp org repo) and now renders in
     commissioned.hbs. No carve-out clause belongs in this line any more.
     Card markup lives in the shared project-cards.hbs partial, which
     commissioned.hbs renders too — the client cards are the SAME size and
     shape; product-first is expressed by order on the page, not card weight.
     ═══════════════════════════════════════════════════════════════════════ --}}

## What I build

<sub>Five products I build and run myself: a news-search tool for AI assistants, a notification system for AI coding tools, a language that turns text into floor plans, an AI design studio for architects, and an AI resume writer.</sub>

{{> project-cards _flagshipProjects}}
