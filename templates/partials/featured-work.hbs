{{!-- ═══════════════════════════════════════════════════════════════════════
     What I build — big editorial cards (vertical stack) for the projects Chan
     owns herself (provenance: personal). Sourced from flagshipProjectIds.
     2026-08-05, second pass: THREE cards. The first pass that day grew the band
     to seven and it read flat — seven cards of equal weight with no clear
     hierarchy. It is now only the work Chan is on full-time, matching
     basics.headline: archlang (the compiler), vitex, archcanvas (the product on
     top of archlang). echook, ai-programming-teaching-project,
     gradient-svg-generator, sunostats and google-news-mcp all moved DOWN to the
     "More I've built" tables in open-source.hbs — a band change, not a demotion.
     The <sub> line below enumerates the cards IN ORDER — it is hand-written,
     so re-read flagshipProjectIds and re-order the clauses whenever that list
     changes, or the prose silently describes the wrong projects.
     2026-07-16: the "All five are live; four are open source" count sentence
     was REMOVED from that line (user-approved) — it had shipped a false
     open-source count three times. If you ever re-add a count, RE-READ
     projects[].repoUrl first: as of 2026-08-05 it is 2 of 3 public, because
     archcanvas is hosted with no public source.
     she-sharp used to be here; it is client work (provenance: client,
     NZ-SheSharp org repo) and now renders in commissioned.hbs. No carve-out
     clause belongs in this line any more.
     Card markup lives in the shared project-cards.hbs partial, which
     commissioned.hbs renders too — the client cards are the SAME size and
     shape; product-first is expressed by order on the page, not card weight.
     ═══════════════════════════════════════════════════════════════════════ --}}

## What I build

<sub>What I work on full-time: a language that turns text into floor plans, the AI design studio built on top of it, and an AI resume writer. Everything else I build is further down.</sub>
{{!-- Clause order above matches flagshipProjectIds: archlang, archcanvas, vitex. --}}

{{> project-cards _flagshipProjects}}
